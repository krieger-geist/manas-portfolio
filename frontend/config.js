/* ==========================================
   API CONFIGURATION
   ------------------------------------------
   This single line controls where the contact
   form sends its requests.

   Local development (default below):
   window.PORTFOLIO_API_URL = "http://localhost:8080";

   Production - once your backend is deployed
   (e.g. to Render/Railway), replace it with that
   URL, then redeploy the frontend:
   window.PORTFOLIO_API_URL = "https://your-backend.onrender.com";
========================================== */
window.PORTFOLIO_API_URL = "https://manas-portfolio-api.onrender.com";
